using System.Collections.Generic;
using Microsoft.VisualBasic;
public class PromptGenerator
{
    public List<string> _prompts;
    string r = Random();
   string myList = new List<string> List; 
  string Parse.count = myList.Count;

  string indexVal = r.Next(count);
  Console.WriteLine(myList[indexVal]);
    public string GetRandomPrompt()
    {
        return "";
    }

}